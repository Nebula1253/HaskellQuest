It all begins with <i>types</i> and <i>expressions</i>...
Types, as you can guess, are how Haskell describes the kind of data you're working with. There are a few standard types in Haskell:
1) Int: an integer within the range -2<sup>29</sup> to (2<sup>29</sup> - 1)
2) Integer: an unbounded integer
3) Float: a floating-point number, and Double: a double-precision float
4) Char: a single character (e.g. 'a', '1', '£')
5) Bool: a boolean value, either True or False
Expressions, meanwhile, are sections of code that can be evaluated to produce a value.
It's like the mathematical definition: 2 + 2 is an expression that yields the value 4.
In Haskell, expressions can be as simple as a single value or as complex as a function call with multiple arguments.
Types and expressions on their own aren't of much use, however, as Haskell doesn't have conventional 'variables': this brings us to functions.
Functions are also like the mathematical definition: they take one or more inputs and return an output value.
Within Haskell, functions need to follow a very specific format.
They need to include a type signature e.g. "incrementInt :: Int -> Int". Here, "Int -> Int" is the function's type: takes an Tnt and returns an Int.
You'd also need to define what the function does in the declaration, e.g. "incrementInt x = x + 1".
A function can also have multiple inputs in Haskell. An example would be "addInts :: Int -> Int -> Int". 
There, since the last type has to be the output, you can tell that the function has two Int inputs.
The function declaration is then, logically, "addInts x y = x + y".
Now, for your journey.
There are going to be obstacles in your path.
Monsters and adversaries that have perverted the power of Haskell for their own gain.
They're going to try and attack you, and you're going to have to learn to defend yourself.
Fortunately, you've been granted the power of the Lambda Gauntlet. 
You can examine the code within your opponents and, most importantly, you can <i>change it</i>.
Use the gauntlet now, and make sure you escape my attack!